import React from 'react';
import ReactDOM from 'react-dom/client';
import "./index.css"

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <div>
      <a className='btn btn-primary' href="#">Seu botão</a>
    </div>
  </React.StrictMode>
);
